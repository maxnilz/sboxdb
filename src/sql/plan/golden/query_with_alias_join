Stmt: 
SELECT
    a.name AS user_name,
    orders.id AS order_id,
    products.name AS product_name
FROM
    users AS a
    JOIN orders ON a.id = orders.user_id
    JOIN products ON orders.product_id = products.id
WHERE
   a.name IN (SELECT name from users WHERE email = 'admin@example.com');        

Logical Plan:
--------------

Projection: a.name AS user_name, orders.id AS order_id, products.name AS product_name
  Filter: a.name IN (Subquery(correlated: false, ...))
    Subquery:
      Projection: users.name
        Filter: users.email = admin@example.com
          TableScan: users
    Inner Join: orders.product_id = products.id
      Inner Join: a.id = orders.user_id
        SubqueryAlias: a
          TableScan: users
        TableScan: orders
      TableScan: products

Logical Plan with schema:
-------------------------

Projection: a.name AS user_name, orders.id AS order_id, products.name AS product_name [user_name:String;N, order_id:Integer;N, product_name:String;N]
  Filter: a.name IN (Subquery(correlated: false, ...)) [id:Integer;N, name:String;N, email:String;N, id:Integer;N, user_id:Integer;N, product_id:Integer;N, amound:Float;N, id:Integer;N, name:String;N]
    Subquery: [name:String;N]
      Projection: users.name [name:String;N]
        Filter: users.email = admin@example.com [id:Integer;N, name:String;N, email:String;N]
          TableScan: users [id:Integer;N, name:String;N, email:String;N]
    Inner Join: orders.product_id = products.id [id:Integer;N, name:String;N, email:String;N, id:Integer;N, user_id:Integer;N, product_id:Integer;N, amound:Float;N, id:Integer;N, name:String;N]
      Inner Join: a.id = orders.user_id [id:Integer;N, name:String;N, email:String;N, id:Integer;N, user_id:Integer;N, product_id:Integer;N, amound:Float;N]
        SubqueryAlias: a [id:Integer;N, name:String;N, email:String;N]
          TableScan: users [id:Integer;N, name:String;N, email:String;N]
        TableScan: orders [id:Integer;N, user_id:Integer;N, product_id:Integer;N, amound:Float;N]
      TableScan: products [id:Integer;N, name:String;N]
